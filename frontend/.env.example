# Socrates Screen – Frontend .env template
# Rename to .env.local (Vite reads .env.local automatically)

# The FastAPI backend base URL.
# In dev, Vite proxies /api/* so this is not strictly needed.
# Set this to your deployed backend URL in production.
VITE_API_BASE=http://localhost:8000
