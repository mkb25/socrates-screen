# Socrates Screen – Backend Python Dependencies
# Install with: pip install -r requirements.txt
# Recommended: Python 3.11+

fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.27.2
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
groq==0.12.0

# Optional: Cerebras inference (faster than Groq on some models)
# cerebras-cloud-sdk==1.15.0

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx  # already listed, used for TestClient too
